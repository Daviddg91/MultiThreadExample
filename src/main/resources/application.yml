server:
  port: 8080
spring:
  kafka:
    partitions: 20
    consumer:
      group-id: group-1
      auto-offset-reset: earliest
      bootstrap-servers: localhost:29092
      enable-auto-commit: false
      topic: "transaction-1"
    listener:
      ack-mode: manual
      concurrency: 100


